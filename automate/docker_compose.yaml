version: '2'
services:
  robot:
    build:
      context: .
      dockerfile: ./Docker_local
    image: robot-results
    volumes:
      - ./results:/app/automate/output:rw
    command: chmod -R 755 ./results
