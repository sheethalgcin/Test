pipeline {
agent any
  stages {
    stage('Robot tests') {
      agent {
       // dockerfile {
        //  filename 'Docker'
          label 'docker'
        //}
      }
      steps {
        sh 'echo $PATH'
        sh "docker --version"
        sh "docker run robot"
      }
    }

  }

}
