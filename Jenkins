pipeline {
agent docker
  stages {
    stage('Robot tests') {
      agent {
       // dockerfile {
        //  filename 'Docker'
          label 'docker'
        //}
      }
      steps {
        sh "docker --version"
        sh "docker run robot"
      }
    }

  }

}
