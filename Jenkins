pipeline {
agent any
  stages {
    stage('Robot tests') {
      agent {
        dockerfile {
          filename 'Docker'
          label 'docker'
          args "-v $WORKSPACE:/workspace"
          additionalBuildArgs "--no-cache"
        }
      }
      steps {
        sh "docker run robot"
      }
    }

  }

}
