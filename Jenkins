
pipeline {
  options {
      timeout(time: 1, unit: 'HOURS')
  }
  agent {
    label 'docker'
  }
  stages {
    stage("Unit tests & linting") {
      agent {
        dockerfile {
          filename 'Dockerfile'
          label 'build150'
         
        }
      }
      steps {
        sh "pytest lib"
        sh 'robocop'
      }
    }
  }
}
